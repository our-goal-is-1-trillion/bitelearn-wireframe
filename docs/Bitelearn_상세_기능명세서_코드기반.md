# Bitelearn 기능 명세서 (코드 기반 정밀 분석본)

> **작성 기준**: 프로젝트의 `App.tsx` 라우팅, 모든 `pages/*.tsx`, `components/features/**/*.tsx` 파일, `data/mock/*.ts` 데이터를 하나하나 열어 실제 렌더링 로직을 확인한 뒤 작성했습니다.

---

## 1. 온보딩 및 인증

| 기능 ID | 기능명                          | 상세 설명                                                                                                                                                                              | 관련 컴포넌트                               |
| :------ | :------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------ |
| ONB_01  | **서비스 온보딩 모달 (4-Step)** | IA 홈에서 트리거하는 **모달**. 전체화면 이미지(`step1~4.png`) + 타이틀 + 본문을 슬라이드형으로 4단계 표시. "다음"/"시작하기" 버튼과 "건너뛰기" 버튼 제공. 하단에 도트 인디케이터 노출. | `OnboardingModal.tsx`, `onboardingData.tsx` |
| ONB_02  | **이메일 로그인**               | E-mail + 비밀번호 폼(zod 유효성 검증). 하단에 구분선("또는") 후 구글/네이버 소셜 로그인 버튼 2개 제공. "계정이 없으신가요? → 회원가입하기" 링크로 회원가입 전환.                       | `Login.tsx`                                 |
| ONB_03  | **회원가입**                    | 이메일 + 비밀번호 + 비밀번호 확인 + 닉네임 4개 필드. zod `.refine()`으로 비밀번호 일치 검증. 완료 시 alert 후 로그인 페이지로 이동.                                                    | `Signup.tsx`                                |

---

## 2. 홈 대시보드 (Tab 1: 홈)

| 기능 ID | 기능명                           | 상세 설명                                                                                                            | 관련 컴포넌트                      |
| :------ | :------------------------------- | :------------------------------------------------------------------------------------------------------------------- | :--------------------------------- |
| DASH_01 | **유저 타입별 동적 헤더**        | `userType`을 `guest` / `new` / `active` 3종으로 분기하여 헤더 메시지와 프로필 진입점을 다르게 렌더링.                | `DashboardHeader.tsx`              |
| DASH_02 | **학습 이어하기 카드**           | `active` 유저에게만 노출. 가장 최근 학습한 레슨의 카테고리·제목·메타 정보를 표시하고, 탭 시 `learningHome`으로 이동. | `DashboardContinueCard.tsx`        |
| DASH_03 | **카테고리 그리드**              | 부동산/금융/커리어/투자 아이콘 그리드. 각 카테고리 클릭 시 연관 학습 화면으로 유도.                                  | `DashboardCategoryList.tsx`        |
| DASH_04 | **오늘의 추천 학습**             | `DashboardTodayRecommendationCard`를 수평 리스트로 렌더링. 카드 탭 시 학습 플로우 진입.                              | `DashboardTodayRecommendation.tsx` |
| DASH_05 | **유용한 지식 아티클 위젯**      | `mockArticles`에서 상위 2건을 `ArticleCard(variant="compact")`로 표시. "전체보기" 버튼으로 아티클 탭 전환.           | `DashboardHome.tsx` 내부           |
| DASH_06 | **하단 GNB (글로벌 네비게이션)** | 홈·학습·노트·아티클·마이 5개 탭. `handleTabClick`으로 앱 전역 화면 전환. 각 탭페이지마다 해당 탭이 `active` 표시됨.  | `DashboardBottomNav.tsx`           |

---

## 3. 학습 (Tab 2: 학습)

### 3-1. 학습 홈 & 챕터 탐색

| 기능 ID | 기능명                                     | 상세 설명                                                                                                                                                                                                                                                                                                                         | 관련 컴포넌트                                   |
| :------ | :----------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------- |
| LRN_01  | **자산 현황 히어로 (통장 뱃지)**           | 유저 누적 EXP에 따라 뱃지를 3단계(🤎낡은 저금통 / 💳든든한 통장 / 💎프리미엄 금고)로 분기 표시. 뱃지 아이콘을 **탭하면 뱃지 컬렉션 모달(Dialog)** 이 열림. 오른쪽에 총 보유 B(바이트) 수치 표시, 하단에 다음 뱃지까지 남은 B와 프로그레스바 노출.                                                                                 | `LearningHome.tsx`, `BadgeCollectionDialog.tsx` |
| LRN_02  | **뱃지 컬렉션 모달 (Dialog)**              | `Dialog` 컴포넌트로 렌더링되는 **모달** 팝업. 자산/도메인 마스터/업적 3개 그룹으로 배지를 나열. 잠금/해제 상태를 시각적으로 구분(해제=검정배경+체크아이콘, 잠금=점선+자물쇠+투명).                                                                                                                                                | `BadgeCollectionDialog.tsx`                     |
| LRN_03  | **지금 당장 이어하기 CTA**                 | 현재 학습 중(`in_progress`) 또는 다음 가용(`available`) 챕터를 자동으로 찾아 카테고리+제목을 CTA 카드로 표시. 탭 시 해당 카테고리의 챕터 목록으로 이동.                                                                                                                                                                           | `LearningHome.tsx`                              |
| LRN_04  | **지식 금고 현황 (카테고리 카드 리스트)**  | 4개 카테고리별로 이모지·카테고리명·태그라인·완료 챕터/전체 챕터·진행률 프로그레스바를 카드로 렌더링. 완료된 카테고리는 "⭐완료" 배지 표시, 미완료 카테고리는 `grayscale` 필터 적용. 탭 시 `ChapterList`로 이동.                                                                                                                   | `LearningHome.tsx` > `CategoryCard`             |
| LRN_05  | **챕터 로드맵 (SVG 커브 + 지그재그 노드)** | SVG `Catmull-Rom → CubicBezier` 곡선을 배경으로 그려 로드맵 경로를 시각화. 노드는 `X_OFFSETS`에 따라 좌우로 지그재그 배치. 노드 상태: ✅완료(검정+체크) / 🔵진행중(파란테두리+핑 애니메이션+"이어서 학습 중" 뱃지) / ⬜대기("다음 챕터") / 🔒잠금(회색+자물쇠, 클릭 불가). 상단에 카테고리명, 태그라인, 전체 진행률(%) 카드 표시. | `ChapterList.tsx`                               |

### 3-2. 챕터 학습 플로우 (ChapterPlayer 4-Phase)

| 기능 ID | 기능명                                | 상세 설명                                                                                                                                                                                                                                                                                            | 관련 컴포넌트                                                               |
| :------ | :------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------- |
| LRN_06  | **챕터 인트로 (Mission Briefing)**    | "CHAPTER MISSION" 라벨 + 챕터 제목 + 🎯이번 미션 설명 + 📖핵심 키워드 칩 리스트. "미션 시작하기" 버튼 탭 시 단어학습 또는 퀴즈로 진입.                                                                                                                                                               | `ChapterIntroScreen.tsx`                                                    |
| LRN_07  | **플래시카드 단어 학습 (스와이프형)** | 앞면(용어+간단 설명) → 탭하면 뒷면으로 3D 플립. `framer-motion` 드래그로 **좌/우 스와이프** 시 이전/다음 카드 이동. 스와이프 시 "알아요✓" / "다음→" 라벨이 카드 모서리에 노출. 하단에 "카드를 뒤집어 확인해 보세요" 가이드 텍스트. 전체 완료 후 "단어 학습 완벽해요!" 브리핑 → "퀴즈 풀러 가기" CTA. | `WordCardsPlayer.tsx`, `WordCard.tsx`                                       |
| LRN_08  | **텍스트 지문 기반 객관식 퀴즈**      | 텍스트 지문(`passage`) + 4개 보기 중 정답 선택. `QuizPlayer`의 `passageMode: "text"` + `choiceMode: "multiple"` 모드로 렌더링.                                                                                                                                                                       | `QuizPlayer.tsx`, `ChoiceQuestionChoices.tsx`                               |
| LRN_09  | **OX 직관 퀴즈**                      | 상황 지문 → O·X 2개의 대형 버튼으로 빠르게 판단. `choiceMode: "ox"` 모드. 별도 래퍼 컴포넌트에서 바텀시트(`OxQuestionBottomSheet`) 또는 인라인 스크롤(`OxQuestionInlineScroll`) 변형도 지원.                                                                                                         | `OxQuestion.tsx`, `OxQuestionBottomSheet.tsx`, `OxQuestionInlineScroll.tsx` |
| LRN_10  | **채팅형 (대화형) 상황 퀴즈**         | 카카오톡 스타일 좌/우 말풍선 UI로 대화 스크립트를 순차 표시한 뒤 문제 출제. `passageMode: "conversation"` 모드.                                                                                                                                                                                      | `ConversationQuestion.tsx`, `ConversationQuestionPassage.tsx`               |
| LRN_11  | **HTML/CSS 서류 판독 객관식 퀴즈**    | `DocumentCard` 컴포넌트로 등기부등본 등 서류 양식을 **정적 이미지 없이 HTML 텍스트+보더 라인**으로 렌더링. 서류의 각 필드(소유자명, 근저당, 가압류 등)를 데이터 바인딩하여 표시. 이 서류를 보고 4지선다 객관식 문제를 풀게 한다. `passageMode: "document"` + `choiceMode: "multiple"`.               | `DocumentChoiceQuestion.tsx`, `DocumentCard.tsx`                            |
| LRN_12  | **서류 필드 직접 터치형 퀴즈**        | DocumentCard 내에서 유저가 **서류의 특정 필드(HTML 요소)를 직접 탭**하여 정답(오류 항목 등)을 선택하는 방식. `choiceMode: "document_select"`. 선택된 필드가 하이라이트되며, 정답/오답 판정 후 해설을 제공.                                                                                           | `DocumentClickQuestion.tsx`, `DocumentCard.tsx`                             |
| LRN_13  | **실시간 정/오답 피드백**             | 문제 풀이 직후 `checking` 단계에서 정/오답을 인라인으로 표시. 바텀시트 또는 인라인 해설을 통해 해설(`explanation`) 렌더링.                                                                                                                                                                           | `QuizPlayer.tsx` (checking phase)                                           |
| LRN_14  | **챕터 완료 화면**                    | 챕터 내 전체 퀴즈 종료 후 "챕터 완료!" 타이틀 + 정답률(%) + 정답 수/전체 수 + 프로그레스바 애니메이션. "최종 결과 확인" 버튼 탭 시 Result 페이지로 이동.                                                                                                                                             | `ChapterDoneScreen.tsx`                                                     |

### 3-3. 최종 결과 (Result)

| 기능 ID | 기능명                                      | 상세 설명                                                                                                                                                           | 관련 컴포넌트     |
| :------ | :------------------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------- |
| RES_01  | **Perfect / Close / Fail 3-분기 결과 화면** | 정답률에 따라 3가지 변형으로 분기 렌더링. 각각 다른 캐릭터 일러스트·이모지·타이틀·설명문·획득/손실 바이트 수를 표시. Perfect 시 🪙코인 파티클 낙하 애니메이션 재생. | `Result.tsx`      |
| RES_02  | **바이트(B) 손익 카드**                     | "지켜낸 바이트"(초록) / "잃어버린 바이트"(빨강) 2칸으로 손익을 시각화. 상단에 "으른 레벨업 게이지" 프로그레스바 표시.                                               | `Result.tsx` 내부 |
| RES_03  | **결과 CTA 분기**                           | Perfect: "다음 챕터 학습하기" 단일 버튼. Close/Fail: "다음 챕터 학습하기" + "오답 풀고 바이트 되찾기" 2개 버튼.                                                     | `Result.tsx` 내부 |

---

## 4. 노트 (Tab 3: 노트)

| 기능 ID | 기능명                          | 상세 설명                                                                                                                                                                                    | 관련 컴포넌트           |
| :------ | :------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------- |
| NOTE_01 | **오답 복습 / 저장한 글 2-Tab** | 상단 탭 전환으로 "오답 복습"과 "저장한 글" 2개의 뷰를 AnimatePresence 슬라이드 전환으로 표시.                                                                                                | `LearningNote.tsx`      |
| NOTE_02 | **바이트 캐릭터 (멍멍이)**      | EXP 기반 3단계(POOR/MIDDLE/WEALTHY) 캐릭터 일러스트. 일러스트가 위아래로 떠다니는 float 애니메이션. 옆에 말풍선으로 오답 수와 함께 메시지 표시(예: "오답 N개를 복습하면 되찾을 수 있어요!"). | `BiteCharacter.tsx`     |
| NOTE_03 | **오답 현황 스탯 카드**         | "복습 대기 N개" + "현재 바이트 N B" 2칸 그리드. 바이트가 음수면 빨간 테마로 변경.                                                                                                            | `LearningNote.tsx` 내부 |
| NOTE_04 | **카테고리별 오답 필터**        | "전체" + 4개 카테고리를 가로 스크롤 칩 버튼으로 제공. 선택 시 해당 카테고리의 오답만 필터.                                                                                                   | `LearningNote.tsx` 내부 |
| NOTE_05 | **오답 카드 목록**              | 카테고리 라벨·날짜·챕터명·문제 텍스트를 카드 형태로 렌더링. 오답이 모두 해소되면 "모든 오답을 정복했어요!" 빈 상태 UI 표시.                                                                  | `MistakeCard.tsx`       |
| NOTE_06 | **저장한 글 (북마크)**          | 북마크된 아티클을 카테고리·제목·썸네일(그레이스케일 처리)과 함께 카드로 표시.                                                                                                                | `LearningNote.tsx` 내부 |

---

## 5. 아티클 (Tab 4: 아티클)

| 기능 ID | 기능명                          | 상세 설명                                                                                                                                                                                                    | 관련 컴포넌트                        |
| :------ | :------------------------------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------- |
| ART_01  | **아티클 목록 & 카테고리 필터** | "전체/부동산·주거/금융·자산/세금·연말정산" 카테고리 칩으로 필터. 첫 번째 아이템은 `HeroArticleCard`(대형 카드+추천 콘텐츠 뱃지)로, 나머지는 일반 `ArticleCard`로 렌더링.                                     | `ArticleList.tsx`, `ArticleCard.tsx` |
| ART_02  | **아티클 상세 (블록형 매거진)** | 앱 내에서 JSON 데이터(`contentBlocks`)를 파싱하여 `paragraph`·`heading`·`image`·`list`·`quote` 5종의 블록을 네이티브 UI로 렌더링. **외부 링크 연결이 아님**. 상단에 커버 이미지+카테고리+제목+작성자 프로필. | `ArticleDetail.tsx`                  |
| ART_03  | **3줄 요약 박스**               | 아티클 상단에 핵심 포인트 3개를 체크 아이콘과 함께 하이라이트 박스로 요약 제공.                                                                                                                              | `ArticleDetail.tsx` 내부             |
| ART_04  | **FAQ 아코디언**                | 아티클 하단에 "궁금증을 해결해드릴게요!" 섹션. 질문 탭 시 답변이 아코디언으로 열림/닫힘. bold 파싱 지원.                                                                                                     | `ArticleDetail.tsx` 내부             |
| ART_05  | **하단 고정 CTA 버튼**          | 아티클 최하단에 Spring 애니메이션으로 슬라이드업 되는 고정 CTA(예: "내 전세집 안전도 1분 만에 진단하기"). primary/secondary 스타일 분기.                                                                     | `ArticleDetail.tsx` 내부             |

---

## 6. 마이페이지 (Tab 5: 마이)

| 기능 ID | 기능명               | 상세 설명                                                                                                                                                                                                                                                                                                                                                           | 관련 컴포넌트       |
| :------ | :------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :------------------ |
| MY_01   | **마이페이지 메인**  | 프로필 카드(아바타 이모지+닉네임+이메일)를 탭하면 계정 정보 서브뷰(`AccountInfoPage`)로 전환 (앱 내부 state 전환, 별도 라우트 아님). 하단에 서비스 약관(이용약관·개인정보 처리방침·마케팅 수신 동의) 메뉴와 고객센터(자주 묻는 질문·1:1 문의·공지사항) 메뉴를 리스트로 표시. **※ 뱃지, 레벨, 경험치 관련 UI는 이 화면에 없음** (학습 탭의 `LearningHome`에서 관리). | `Mypage.tsx`        |
| MY_02   | **계정 정보 서브뷰** | 프로필 이미지(카메라 아이콘으로 변경 UI) + 닉네임(✏편집 아이콘) + 이메일(읽기 전용) + 비밀번호 변경 링크 + "로그아웃" 버튼 + "회원 탈퇴" 버튼(빨강). 뒤로가기 시 마이페이지 메인으로 복귀.                                                                                                                                                                          | `MyAccountInfo.tsx` |

---

## 7. 공통 시스템

| 기능 ID | 기능명                        | 상세 설명                                                                                                                                                                                             | 관련 컴포넌트                 |
| :------ | :---------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------- |
| SYS_01  | **IA 네비게이션 홈 (개발용)** | 트리 형태로 전체 앱의 화면 구조를 나열하고 각 항목 옆의 "GO" 버튼으로 해당 화면에 직접 진입할 수 있는 **개발/프레젠테이션용** 루트 화면. 유저 스토리·기능명세서 바로가기 버튼, 앱 갤러리 버튼도 제공. | `Home.tsx`                    |
| SYS_02  | **페이지 트랜지션**           | `transitionStage`(out→in→idle) 상태 관리를 통해 180ms fade-out + 260ms fade-in 전환 애니메이션 적용.                                                                                                  | `App.tsx`                     |
| SYS_03  | **퀴즈 변형 레이아웃 래퍼**   | 객관식/OX 퀴즈의 바리에이션(기본·바텀시트·인라인스크롤)을 하나의 `QuizLayoutWrapper`로 묶어 상단에 변형 선택 바 제공.                                                                                 | `QuizLayoutWrapper.tsx`       |
| SYS_04  | **통합 스텝 인디케이터**      | `ChapterPlayer`에서 단어 스텝과 퀴즈 스텝을 하나의 연속 인디케이터 바로 통합 표시. 각 스텝에 `word`/`quiz` 타입과 `none`/`correct`/`incorrect` 상태 표시.                                             | `ChoiceQuestionIndicator.tsx` |
| SYS_05  | **앱 갤러리**                 | 전체 화면들을 모아보는 갤러리 뷰. 각 화면의 `demoState`를 주입하여 특정 상태의 스냅샷을 보여줌.                                                                                                       | `AppGallery.tsx`              |
